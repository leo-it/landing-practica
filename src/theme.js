import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  status: {
    danger: '#e53e3e',
  },
  palette: {
    neutral: {
      main: '#A8A7A6',
      contrastText: '#fff',
    },
    black: {
      main: '#DBDBDB',
      contrastText: '#000000',
    },
  },
});